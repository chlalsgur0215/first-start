#include <iostream>
using namespace std;
int main()
{
    int A, B;
    cin >> A >> B;
    cout << A + B;
}
//cin = scanf() / cout = printf() 
// >> , << 로 방향 지정 A+B를 출력한다 ? cout으로 보내기 , 값을 넣는다? >> 로 변수에 넣기
